# Make sure to check the documentation at https://goreleaser.com
project_name: cli-plugin-gcp
before:
  hooks:
    - go mod tidy
builds:
  - id: "gcp"
    binary: "gcp"
    tags:
      - "gcp"
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    ldflags:
      - -X main.version={{.Version}}
    main: './main_gcp.go'
  - id: "bigquery"
    binary: "bigquery"
    tags:
      - "bigquery"
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    ldflags:
      - -X main.version={{.Version}}
    main: './main_bigquery.go'

checksum:
  name_template: 'checksums.txt'
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
archives:
  - builds:
      - "gcp"
      - "bigquery"
    format: 'tar.gz'
    name_template: '{{ .ProjectName }}-{{ .Binary }}-{{ .Version }}-{{ .Os }}_{{ .Arch }}'
  - builds:
    - "gcp"
    format: 'tar.gz'
    name_template: '{{ .ProjectName }}-{{ .Version }}-{{ .Os }}_{{ .Arch }}'
release:
  prerelease: auto